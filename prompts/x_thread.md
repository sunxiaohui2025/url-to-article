# X 推文串整合（宿主 agent 执行）

当提取结果 `is_thread == true` 且有多条推文时，将推文串整合成连贯文章。
素材源为 `output/extract_*.json` 中的 `tweets` 数组（含 order 与 text）。
若只有单条推文，则直接用 `full_text`，无需整合。

要求：
1. 保持原意，不删减任何技术细节
2. 去除「🧵」「1/」「2/」等符号，转为文章风格
3. 保持逻辑清晰、段落分明
4. 推文较多时添加合适的小标题
5. 不要添加自己的评论或解释
6. 直接输出整合后的纯文本文章
