# ============================================================
# 环境变量模板（可选）
#
# 本 skill 只做抓取与解析，不直连任何大模型。
# 翻译 / HTML 生成等 LLM 任务由宿主 agent 完成，无需（也不应）
# 在此配置任何模型地址、名称或 API 密钥。
#
# 目前代码不依赖任何环境变量，此模板留空即可。
# 如未来需要自定义运行参数，可在此添加，并把占位值写入 .env
# （.env 已被 .gitignore 排除，不会提交到公共仓库）。
# ============================================================
