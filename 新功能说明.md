# 新功能说明 - Banner自动保存

## 🎯 主要改进

### 1. Banner文件自动保存

现在系统会自动保存两个Banner：

**Banner 1: 图片Banner** (`output/banners/xxx_banner_image.json`)
- 如果找到大于640的图片，自动保存图片信息
- JSON格式，包含URL和尺寸
- 可以直接读取并在HTML中使用

**Banner 2: SVG Banner** (`output/banners/xxx_banner_svg.svg`)
- 通过LLM生成（需要调用API）
- 直接保存为SVG文件
- 可以直接嵌入HTML或单独使用

### 2. 一页纸HTML自动填充Banner

生成一页纸解读HTML时，会自动将SVG Banner填充到 `<!--BANNER_SLOT-->` 位置。

优先级：**SVG Banner > 图片Banner > 无Banner**

## 📁 输出目录结构

```
output/
├── extract_xxx.json          # 提取的原始数据
├── extract_xxx.md            # 可读的素材文件
├── banners/                  # 新增：Banner目录
│   ├── extract_xxx_banner_image.json  # 图片Banner信息
│   └── extract_xxx_banner_svg.svg     # SVG Banner
└── html/                     # 新增：HTML目录
    └── extract_xxx_summary.html       # 一页纸解读（已填充Banner）
```

## 🚀 使用方式

### 方式1: 只提取内容（不调用LLM）

```bash
python -m src.main "https://example.com/article"
```

**输出：**
- 提取的JSON和Markdown
- 如果有大图，保存Banner 1 (图片信息)
- 不生成SVG Banner和HTML（需要LLM）

### 方式2: 完整工作流（集成LLM）

```bash
python run_with_llm.py "https://example.com/article"
```

**需要先配置：**
1. 编辑 `run_with_llm.py`
2. 实现 `call_llm()` 函数
3. 配置API密钥

**输出：**
- ✅ 提取的JSON和Markdown
- ✅ Banner 1 (图片信息，如果有)
- ✅ Banner 2 (SVG文件)
- ✅ 一页纸解读HTML（已自动填充SVG Banner）

### 方式3: 使用工作流类（编程方式）

```python
from src.workflow import ArticleWorkflow

workflow = ArticleWorkflow()

result = workflow.process(
    url="https://example.com/article",
    llm_translate_func=your_translate_func,      # 可选
    llm_summary_html_func=your_html_func,        # 可选
    llm_banner_svg_func=your_svg_func            # 可选
)

# 查看生成的文件
print(result['banners']['saved_files'])
# {'banner_image': 'output/banners/xxx_banner_image.json',
#  'banner_svg': 'output/banners/xxx_banner_svg.svg'}
```

## 📝 返回数据结构

```python
{
    'success': True,
    'extraction': {
        'full_text': '...',
        'large_image': {
            'url': '...',
            'width': 1200,
            'height': 800
        },
        'saved_files': {
            'json': 'output/extract_xxx.json',
            'markdown': 'output/extract_xxx.md',
            'banner_image': 'output/banners/extract_xxx_banner_image.json'  # 新增
        }
    },
    'banners': {
        'large_image': {...},      # 图片信息
        'svg_banner': '...',       # SVG代码
        'saved_files': {           # 新增：Banner文件路径
            'banner_image': 'output/banners/xxx_banner_image.json',
            'banner_svg': 'output/banners/xxx_banner_svg.svg'
        }
    },
    'html': {
        'summary': 'output/html/xxx_summary.html'  # 已填充Banner
    },
    'all_files': {  # 所有生成的文件路径
        'json': '...',
        'markdown': '...',
        'banner_image': '...',
        'banner_svg': '...',
        'summary_html': '...'
    }
}
```

## 🎨 Banner使用示例

### 读取图片Banner

```python
import json

with open('output/banners/extract_xxx_banner_image.json', 'r') as f:
    banner = json.load(f)
    print(f"URL: {banner['url']}")
    print(f"尺寸: {banner['width']}x{banner['height']}")

# 在HTML中使用
html = f'<img src="{banner["url"]}" alt="Banner" style="width: 100%;" />'
```

### 读取SVG Banner

```python
with open('output/banners/extract_xxx_banner_svg.svg', 'r') as f:
    svg = f.read()

# 直接嵌入HTML
html = f'<div>{svg}</div>'
```

## 🔧 自定义配置

### 修改Banner优先级

在 `src/workflow.py` 中：

```python
# 生成用于插入HTML的Banner代码
banner_html = self.banner_gen.generate_banner_html(
    large_image=result.get('large_image'),
    svg_banner=svg_banner,
    prefer_image=False  # 改为 True 则优先使用图片
)
```

### 自定义Banner保存位置

在 `src/config.py` 中：

```python
class Config:
    OUTPUT_DIR = BASE_DIR / "output"
    BANNER_DIR = OUTPUT_DIR / "banners"  # 新增
    HTML_DIR = OUTPUT_DIR / "html"       # 新增
```

## ⚠️ 注意事项

1. **Banner 1 (图片) 只是保存URL信息**
   - 不会下载图片到本地
   - 需要网络访问才能显示
   - 如果需要本地保存，需要自己下载

2. **Banner 2 (SVG) 需要LLM生成**
   - 基础提取流程不会生成
   - 需要调用 `workflow.process()` 并提供 `llm_banner_svg_func`

3. **HTML自动填充**
   - 只在使用 `HtmlGenerator.save_summary_html()` 时自动填充
   - 如果手动生成HTML，需要自己替换占位符

## 📚 相关文件

| 文件 | 说明 |
|-----|------|
| `src/banner_generator.py` | Banner生成和保存逻辑 |
| `src/html_generator.py` | HTML生成和Banner填充逻辑 |
| `src/workflow.py` | 完整工作流协调器 |
| `run_with_llm.py` | 集成LLM的完整示例 |

## 🎉 总结

现在你不需要手动处理Banner了：

✅ Banner 1 (图片) - 自动保存JSON文件  
✅ Banner 2 (SVG) - 自动保存SVG文件  
✅ 一页纸HTML - 自动填充SVG Banner  
✅ 所有文件路径 - 在返回结果中可以获取  

直接使用生成的文件即可！
